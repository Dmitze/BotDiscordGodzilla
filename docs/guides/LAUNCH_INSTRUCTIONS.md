# 🚀 Інструкції по запуску доробленого проекту

## 📋 Що було додано в версії 2.1.0

### ✨ Нові функції:
- 📊 **Система статистики** - відстеження використання бота
- 🏆 **Топ команд** - найпопулярніші команди
- 👥 **Топ користувачів** - найактивніші користувачі
- 🔧 **Статус системи** - моніторинг працездатності
- 🤖 **Покращений AI** - розширений функціонал

### 🔧 Покращення:
- ✅ Виправлено Git конфлікти в PowerShell скрипті
- 📝 Створено шаблон `.env` файлу
- 🔄 Автоматичне оновлення slash-команд
- 📊 Логування статистики та помилок

## 🚀 Кроки для запуску

### 1. Підготовка середовища

#### Створіть файл `.env` на основі `env.example`:
```bash
# Скопіюйте env.example в .env
cp env.example .env
```

#### Заповніть `.env` файл:
```env
# Discord Bot Configuration
BOT_TOKEN=your_discord_bot_token_here

# Google Sheets Configuration
SHEET_ID=your_google_sheet_id_here
GOOGLE_API_KEY=your_google_api_key_here
APP_SCRIPT_URL=your_google_apps_script_url_here

# OpenAI Configuration (для AI-функціоналу)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Logging Configuration
LOG_LEVEL=info

# Optional: Bot Configuration
BOT_PREFIX=!
GUILD_ID=your_guild_id_here

# Optional: Performance Configuration
CACHE_TTL=300000
MAX_SEARCH_RESULTS=20
MAX_ANALYSIS_ROWS=50
```

### 2. Встановлення залежностей

```bash
# Встановлюємо залежності
npm install

# Перевіряємо, що все встановлено
npm list
```

### 3. Оновлення slash-команд

```bash
# Оновлюємо команди в Discord
node deploy-commands.js
```

**Очікуваний результат:**
```
🔄 Початок оновлення slash-команд...
✅ Slash-команди успішно оновлені!
📊 Всього команд: 15

📋 Список команд:
1. /залишки - Показує підсумкові значення з таблиці
2. /оновити - Показує останні 10 записів з таблиці
...
```

### 4. Тестування AI-функціоналу

```bash
# Тестуємо AI-функції
node test-ai.js
```

**Очікуваний результат:**
```
🤖 Тестування AI-функціоналу...

📊 Тест 1: AI-аналіз даних
[результат аналізу]

🔍 Тест 2: Природномовний пошук
Результати пошуку: X
Пояснення: [пояснення]

💡 Тест 3: AI-рекомендації
[рекомендації]

📋 Тест 4: AI-звіт
[звіт]
```

### 5. Запуск бота

#### Варіант 1: PowerShell скрипт (рекомендовано)
```powershell
# Запускаємо через PowerShell
.\запуск-бота.ps1
```

#### Варіант 2: Прямий запуск
```bash
# Запускаємо бота
node index.js
```

**Очікуваний результат:**
```
🤖 Запускаємо Discord бота...
Бот BotName#1234 онлайн!
Slash-команди зареєстровані!
```

## 🧪 Тестування нових функцій

### 1. Тестування статистики

Використайте команди в Discord:
```
/статистика
/топ-команди
/топ-користувачі
/статус
```

### 2. Тестування AI-функцій

```
/ai-аналіз
/ai-пошук запит:покажи товари дешевше 1000 грн
/ai-рекомендації
/ai-звіт тип:general
```

### 3. Тестування основних функцій

```
/залишки
/оновити
/пошук поле:назва запит:стол
/help
```

## 🔧 Вирішення проблем

### Проблема: "Одна з обов'язкових ENV-змінних відсутня"
**Рішення:** Перевірте файл `.env` та заповніть всі необхідні змінні

### Проблема: "Slash-команди не оновлюються"
**Рішення:** Запустіть `node deploy-commands.js` та перевірте права бота

### Проблема: "AI-функції не працюють"
**Рішення:** 
1. Перевірте `OPENAI_API_KEY` в `.env`
2. Запустіть `node test-ai.js`
3. Перевірте баланс на OpenAI рахунку

### Проблема: "Помилка підключення до Google Sheets"
**Рішення:**
1. Перевірте `SHEET_ID` та `GOOGLE_API_KEY`
2. Переконайтеся, що Google Sheets API увімкнено
3. Перевірте права доступу до таблиці

## 📊 Моніторинг

### Перевірка логів:
```bash
# Переглядаємо логи
tail -f logs/combined.log
tail -f logs/error.log
```

### Перевірка статистики:
```bash
# Переглядаємо файл статистики
cat logs/stats.json
```

## 🔄 Оновлення проекту

### Отримання оновлень:
```bash
git pull origin main
npm install
node deploy-commands.js
```

### Скидання статистики (за потреби):
```bash
# Відредагуйте stats.js та додайте метод resetStats()
# Або видаліть файл logs/stats.json
```

## 📞 Підтримка

### Якщо щось не працює:
1. Перевірте логи в папці `logs/`
2. Використайте команду `/статус`
3. Запустіть тести: `node test-ai.js`
4. Перевірте налаштування в `.env`

### Корисні команди для діагностики:
```bash
# Перевірка версії Node.js
node --version

# Перевірка залежностей
npm list

# Перевірка синтаксису
node -c index.js
node -c stats.js
node -c aiHelpers.js

# Тестування AI
node test-ai.js

# Оновлення команд
node deploy-commands.js
```

---

**🎉 Вітаємо! Ваш Discord бот тепер має повноцінну систему статистики та покращений AI-функціонал!** 